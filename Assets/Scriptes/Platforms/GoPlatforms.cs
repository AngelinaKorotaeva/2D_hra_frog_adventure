using UnityEngine;


    public class GoPlatforms : MonoBehaviour
    {
    //[SerializeField] private float platformSpeed = -2f;

    //[SerializeField] private float resistanceForce = 10f;
    //[SerializeField] private float boostForce = 10f;

    //private void OnTriggerStay2D(Collider2D other)
    //{
    //    if (other.CompareTag("Player"))
    //    {
    //
    //        Rigidbody2D rb = other.attachedRigidbody;
    //        if (rb != null)
    //        {
    //            float vx = rb.linearVelocity.x;

    //            if (Mathf.Abs(vx) < 0.1f)
    //            {
    //                rb.AddForce(Vector2.right * platformSpeed, ForceMode2D.Force);
    //            }
    //            else if (vx > 0.1f)
    //            {
    //                rb.AddForce(Vector2.left * resistanceForce, ForceMode2D.Force);
    //            }
    //            else if (vx < -0.1f)
    //            {
    //                rb.AddForce(Vector2.right * boostForce, ForceMode2D.Force);
    //            }
    //        }
    //
    //    }
    //}
}

